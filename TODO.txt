- Install Ultralytics YOLOv5/YOLOv8 (done).
- Convert synthetic data in `output/data.json` to COCO format (done).
- Obtain or create an Ultralytics config file and update paths to point to the local data.
- Replace the dummy model and training logic in `mmyolo_training.py` with Ultralytics training API.
- Test the training setup with a small dataset to ensure it runs without errors.